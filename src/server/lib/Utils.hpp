#include <string>
#include <vector>
#include <sstream>
#include "./Client.hpp"


using namespace std;

// Quebra a string em um vetor de strings com base no delimitador passado
vector<string>split(string s, char delimiter);

string message_string(string msg);